# Work Log: 2026-03-23

## Starting state
- 4 sorries (resolventMapsToDomain, resolventRightInv, 2× BCR)
- Helper lemmas not yet written

## What was done

### Forward Hille-Yosida: COMPLETE (0 sorries)
- Proved `integral_comp_add_right_Ioi` via indicator + `integral_add_right_eq_self`
- Proved `integral_Ioi_eq_Ioc_add_Ioi` via `setIntegral_union`
- Proved `h_push` (integral shift): `ContinuousLinearMap.integral_comp_comm` + semigroup + `mul_smul`/`exp_add`/`ring` + translation
- Proved `h_split`, `h_identity` (algebraic steps)
- Proved exp derivative limit: `HasDerivAt.tendsto_slope_zero_right`
- Proved exp continuity: `hderiv.continuousAt.tendsto`
- Proved FTC via `integral_hasDerivAt_of_tendsto_ae_right` with piecewise function trick:
  - `hg_cont` (piecewise continuity): `nhdsLT_sup_nhdsGE` + `Tendsto.smul` + `strong_cont`
  - `hg_continuous` (global continuity): `continuous_piecewise` + `frontier_Ici_subset` + `closure_Ici`
  - `StronglyMeasurableAtFilter`: from `Continuous.stronglyMeasurableAtFilter`
- Refactored: extracted `resolvent_generator_tendsto` as shared helper
- Proved `resolventMapsToDomain`: one-liner `⟨_, resolvent_generator_tendsto⟩`
- Proved `resolventRightInv`: `tendsto_nhds_unique` + `abel`

### Toolchain upgrade
- Upgraded from v4.28.0 to v4.29.0-rc6 (matches bochner repo)
- Added `BochnerMinlos` as lake dependency from `mrdouglasny/bochner`
- All files build clean on new toolchain

### BCR decomposition
- Stated `IsCompletelyMonotone` definition (forward differences)
- Stated `bernstein_theorem` as axiom
- Proved `spatial_slice_pd.nonneg`: negate spatial arguments trick + `simp_rw`
- `spatial_slice_pd.hermitian`: sorry (Fin 2 conjugate symmetry argument)
- Structured `semigroupGroupBochnerExtension`: obtain μ, define G, 3 sub-goals
- Documented full 7-step BCR = Bochner + Bernstein decomposition

### Future axioms
- Added `Future/GenerationTheorem.lean`: converse Hille-Yosida (Lumer-Phillips)
  - `DenseLinearOperator` structure
  - `IsDissipative` definition
  - `hilleYosidaGeneration` axiom
  - `domain_isDense` axiom

### Documentation
- LaTeX formulas in README theorem statement and proof outline
- Updated docs/proof-outline.md with full LaTeX
- Updated history

## Current state
- `StronglyContinuousSemigroup.lean`: **0 sorries** (forward H-Y complete)
- `SemigroupGroupExtension.lean`: **2 sorry warnings** (BCR axiom cascade)
- `Future/GenerationTheorem.lean`: 3 axioms (converse H-Y, domain density)
- `Future/BernsteinTheorem.lean`: 1 axiom (Bernstein) + 1 sorry (hermitian) + bridge lemma

## Key techniques discovered
- `simp_rw` for rewriting under sum binders (vs `simp only` which fails)
- `funext` to create function equalities for `simp` consumption
- Negate spatial arguments to swap index convention in PD
- `continuous_piecewise` + `frontier_Ici_subset` for measurability
- `nhdsLT_sup_nhdsGE` to split nhds into left + right
