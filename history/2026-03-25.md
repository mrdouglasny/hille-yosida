# Work Log: 2026-03-25

## Starting state
- 2 sorry warnings (Bernstein.lean: `deriv_cm` + `bernstein_theorem`)
- `bernstein_theorem` was axiom, then theorem with `exact sorry`

## What was done

### Proved CM helper lemmas
- `bounded`: `f(t) ≤ f(0)` via `antitoneOn_of_deriv_nonpos` + MVT
- `hρ_nonneg`: density nonnegativity via `field_simp` rearrangement
- `nonneg`: cleaned with `simpa`
- `deriv_cm_sign`: sign condition via `iteratedDerivWithin_fun_neg` + `succ'`

### Proved taylor_integral_remainder (NEW, not in Mathlib)
- `f(b) - T_n(a,b) = ∫_a^b (b-t)^n/n! f^{(n+1)}(t) dt`
- Via FTC: `taylorWithinEval_self` + `hasDerivWithinAt_taylorWithinEval_at_Icc`
  + `integral_eq_sub_of_hasDerivAt_of_le`

### Proved CM-Taylor connection
- `taylor_nonneg_remainder`: CM sign for Taylor integral (ae argument on Ioo)
- `iteratedDerivWithin_Icc_eq_Ici`: set transfer at interior points
- `integral_neg_deriv`: `f(x) - f(T) = ∫ (-f')`
- `integral_neg_deriv_Ici`: T-independent set transfer for integrals
- `integral_mass`: `∫_0^T (-f') = f(0) - f(T)`

### Proved convergence at infinity
- `tendsto_atTop`: `f(t) → L ≥ 0` via `tendsto_atTop_ciInf` + `Tendsto.congr'`
- `tendsto_total_mass`: `∫_0^T (-f') → f(0) - L`
- `neg_deriv_integrableOn`: `-f'` integrable on `(0,∞)` via `integrableOn_Ioi_of_intervalIntegral_norm_bounded`
- `integral_Ioi_neg_deriv`: `∫_(0,∞) (-f') = f(0) - L` via `intervalIntegral_tendsto_integral_Ioi` + `tendsto_nhds_unique`

### Measure construction
- `cm_density`, `cm_measure`: Chafaï density and measure definitions
- `cm_density_nonneg`, `cm_density_one`: density properties
- `bernstein_packaging`: `μ₀ + L·δ₀` construction (fully proved incl. integral split via `@integral_smul_measure`)

### Prokhorov infrastructure (via parallel agents)
- `cm_rescaled`: pushforward measure σ̃_n
- `cm_rescaling_measurable`, `cm_measure_compl_Ioi`, `cm_rescaled_Iio_zero`, `cm_rescaled_mass_eq`
- `bernstein_kernel`, `bernstein_kernel_tendsto`: kernel `(1-xp/(n-1))^{n-1} → e^{-xp}` (sorry-free!)
- `cm_prokhorov_and_verify`: assembled from 4 sub-lemmas (sorry-free assembly)
- `cm_measure_finite_mass`: `IsFiniteMeasure` + mass bound proved (modulo IBP hbound)

### Proof architecture
```
bernstein_theorem = tendsto_atTop + neg_deriv_integrableOn + integral_Ioi_neg_deriv
  + cm_laplace_representation (= cm_measure_finite_mass + cm_prokhorov_and_verify)
  + bernstein_packaging
```
All non-leaf nodes sorry-free.

## Final state
- 729 lines, 35+ declarations
- 5 sorry's in 5 leaf lemmas:
  1. `hbound` (IBP recursion: ∫ ρ_n ≤ f(0)-L)
  2. `cm_cov_identity` (change of variables)
  3. `cm_taylor_poly_tendsto` (Taylor poly → L)
  4. `cm_prokhorov_extract` (Prokhorov compactness)
  5. `cm_limit_identification` (Portmanteau verification)
- 3 parallel agents launched to fill sorry's 1, 4, 5

### IBP agent results
- `cm_density_ibp_identity`: IBP recursion I_k(T) = B_k(T) + I_{k-1}(T) via FTC
- `cm_density_ibp_step`: B_k(T) ≤ 0 by CM sign → I_k ≤ I_{k-1}
- These fill `hbound` sorry: by induction I_n ≤ I_1 = f(0)-f(T) ≤ f(0)-L

### Riesz-Markov agent results
- `bernstein_kernel_tendsto`: (1-xp/(n-1))^{n-1} → e^{-xp} (sorry-free!)
  Uses `Real.tendsto_one_add_div_pow_exp` from Mathlib
- Decomposed Prokhorov step into 4 sub-lemmas (2 had incorrect statements, fixed later)

### Bug found and fixed
- `cm_cov_identity` statement was FALSE: Bernstein integral over (x,∞) ≠ Taylor remainder over (0,x)
- `cm_taylor_poly_tendsto` statement was FALSE: Taylor poly at 0 → f(x), not L
  Counterexample: f(t) = 1/(1+t) (Taylor series diverges for x > 1)
- Fixed: replaced 4 incorrect sub-lemmas with correct `chafai_identity` + `prokhorov_limit_identification`

### Final decomposition
- `cm_prokhorov_and_verify` proved from `chafai_identity` + `prokhorov_limit_identification`
- `cm_rescaled_isFiniteMeasure` proved
- Prokhorov agent found Mathlib API gap: `LevyProkhorov` type synonym needed for sequential compactness

## Session 1 final state
- 1178 lines, ~48 declarations, 4 sorry's
- Sorry 1: `chafai_repeated_ibp` inductive step (IBP + boundary decay, ~30 lines)
- Sorry 2: Measurability of `ENNReal.ofReal ∘ cm_density` (minor technicality)
- Sorry 3: `finite_measure_subseq_limit` (Prokhorov extraction, LevyProkhorov API)
- Sorry 4: `kernel_approx_error_tendsto` (tightness + uniform convergence)

### Proved in main:
- `chafai_kernel_density_eq` (change of variables, ~70 lines, sorry-free)
- `chafai_repeated_ibp` n=1 base case (FTC + set transfer, sorry-free)
- `chafai_identity` assembly (steps 1-4, sorry-free modulo sub-lemmas)
- `exp_bcf` + `tendsto_exp_integral` + `diagonal_convergence` (sorry-free)
- `prokhorov_limit_identification` assembly (sorry-free modulo sub-lemmas)
- `cm_prokhorov_and_verify` (sorry-free from chafai_identity + prokhorov_limit_id)

---

## Session 2: Prokhorov extraction → Bernstein complete

### Prokhorov extraction (`finite_measure_subseq_limit`)
- `normalize_le`: `↑(normalize μ)(A) ≤ ↑μ(A)` when mass ≥ 1, via `inv_le_one_of_one_le₀`
- `prob_seq_compact`: `ProbabilityMeasure ℝ` metrizable via Lévy-Prokhorov → `isCompact_iff_isSeqCompact` applies
- Steps 1+3: normalization + mass recovery proved
- Steps 2b+4: tightness hypothesis + σ recovery sorry'd

### Tightness and integral bounds
- `hν_ne`, `hν_mass`: ν_n ≠ 0, mass(ν_n) ≥ 1
- Complement bound: `(σ+δ)(K^c) ≤ σ(Ioi R) ≤ ε`
- `hmass_real`: mass identity via `toReal` conversion
- `tail_setIntegral_tendsto_zero` helper
- x₀ choice: `ContinuousWithinAt` + dist bound
- `h_diff`: integrability of difference in tightness bound
- `(1-u/n)^n ≤ e^{-u}`: via `Real.add_pow_le_exp_nmul`
- `exp(-xR) → 0`: `tendsto_exp_neg_atTop_nhds_zero.comp`

### Codex fills 3 sorry's
- `chafai_repeated_ibp` inductive step
- Measurability of `ENNReal.ofReal ∘ cm_density`
- Integrability + uniform convergence (`kernel_approx_error_tendsto`)
- Down to 1 sorry: `finite_measure_subseq_limit`

### Bernstein COMPLETE (4ec3ccc)
- Last sorry `finite_measure_subseq_limit` filled
- **`bernstein_theorem`: 2444 lines, 0 sorry's, 0 axioms**

### BCR d=0 started
- `BCR_d0.lean` created with full architecture
- `iterForwardDiff_eq_sum`, `abs_le` proved
- `alternating_forwardDiff`: PD → alternating differences via Vandermonde convolution
- `pd_quadratic_form_of_measure` proved (was axiom)
- `semigroupGroupBochner` decomposed into 4 sorry lemmas

## Session 2 final state
- Bernstein.lean: **0 sorry's** (DONE)
- BCR_d0.lean: new file, architecture in place, multiple sorry's
- BCR_General.lean (then Future/BernsteinTheorem.lean): axioms being eliminated

## Key techniques
- `@integral_smul_measure` with explicit args for `∂(c • μ)` parsing
- `Ioo_ae_eq_Icc` + `ae_restrict_iff'` for ae on intervals
- `integrableOn_Ioi_of_intervalIntegral_norm_bounded` for improper integrability
- `isFiniteMeasure_withDensity_ofReal` for finite measure from integrable density
- `ofReal_integral_eq_lintegral_ofReal` for lintegral-integral conversion
- `Real.tendsto_one_add_div_pow_exp` for kernel convergence
- `FiniteMeasure.normalize` → `ProbabilityMeasure` for Prokhorov compactness
- `LevyProkhorov` type synonym for `PseudoMetrizableSpace` instance
