# Work Log: 2026-03-25

## Starting state
- 2 sorry warnings (Bernstein.lean: `deriv_cm` + `bernstein_theorem`)
- `bernstein_theorem` was axiom, then theorem with `exact sorry`

## What was done

### Proved CM helper lemmas
- `bounded`: `f(t) ≤ f(0)` via `antitoneOn_of_deriv_nonpos` + MVT
- `hρ_nonneg`: density nonnegativity via `field_simp` rearrangement
- `nonneg`: cleaned with `simpa`
- `deriv_cm_sign`: sign condition via `iteratedDerivWithin_fun_neg` + `succ'`

### Proved taylor_integral_remainder (NEW, not in Mathlib)
- `f(b) - T_n(a,b) = ∫_a^b (b-t)^n/n! f^{(n+1)}(t) dt`
- Via FTC: `taylorWithinEval_self` + `hasDerivWithinAt_taylorWithinEval_at_Icc`
  + `integral_eq_sub_of_hasDerivAt_of_le`

### Proved CM-Taylor connection
- `taylor_nonneg_remainder`: CM sign for Taylor integral (ae argument on Ioo)
- `iteratedDerivWithin_Icc_eq_Ici`: set transfer at interior points
- `integral_neg_deriv`: `f(x) - f(T) = ∫ (-f')`
- `integral_neg_deriv_Ici`: T-independent set transfer for integrals
- `integral_mass`: `∫_0^T (-f') = f(0) - f(T)`

### Proved convergence at infinity
- `tendsto_atTop`: `f(t) → L ≥ 0` via `tendsto_atTop_ciInf` + `Tendsto.congr'`
- `tendsto_total_mass`: `∫_0^T (-f') → f(0) - L`
- `neg_deriv_integrableOn`: `-f'` integrable on `(0,∞)` via `integrableOn_Ioi_of_intervalIntegral_norm_bounded`
- `integral_Ioi_neg_deriv`: `∫_(0,∞) (-f') = f(0) - L` via `intervalIntegral_tendsto_integral_Ioi` + `tendsto_nhds_unique`

### Measure construction
- `cm_density`, `cm_measure`: Chafaï density and measure definitions
- `cm_density_nonneg`, `cm_density_one`: density properties
- `bernstein_packaging`: `μ₀ + L·δ₀` construction (fully proved incl. integral split via `@integral_smul_measure`)

### Prokhorov infrastructure (via parallel agents)
- `cm_rescaled`: pushforward measure σ̃_n
- `cm_rescaling_measurable`, `cm_measure_compl_Ioi`, `cm_rescaled_Iio_zero`, `cm_rescaled_mass_eq`
- `bernstein_kernel`, `bernstein_kernel_tendsto`: kernel `(1-xp/(n-1))^{n-1} → e^{-xp}` (sorry-free!)
- `cm_prokhorov_and_verify`: assembled from 4 sub-lemmas (sorry-free assembly)
- `cm_measure_finite_mass`: `IsFiniteMeasure` + mass bound proved (modulo IBP hbound)

### Proof architecture
```
bernstein_theorem = tendsto_atTop + neg_deriv_integrableOn + integral_Ioi_neg_deriv
  + cm_laplace_representation (= cm_measure_finite_mass + cm_prokhorov_and_verify)
  + bernstein_packaging
```
All non-leaf nodes sorry-free.

## Final state
- 729 lines, 35+ declarations
- 5 sorry's in 5 leaf lemmas:
  1. `hbound` (IBP recursion: ∫ ρ_n ≤ f(0)-L)
  2. `cm_cov_identity` (change of variables)
  3. `cm_taylor_poly_tendsto` (Taylor poly → L)
  4. `cm_prokhorov_extract` (Prokhorov compactness)
  5. `cm_limit_identification` (Portmanteau verification)
- 3 parallel agents launched to fill sorry's 1, 4, 5

## Key techniques
- `@integral_smul_measure` with explicit args for `∂(c • μ)` parsing
- `Ioo_ae_eq_Icc` + `ae_restrict_iff'` for ae on intervals
- `integrableOn_Ioi_of_intervalIntegral_norm_bounded` for improper integrability
- `isFiniteMeasure_withDensity_ofReal` for finite measure from integrable density
- `ofReal_integral_eq_lintegral_ofReal` for lintegral-integral conversion
- `Real.tendsto_one_add_div_pow_exp` for kernel convergence
