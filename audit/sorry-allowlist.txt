# audit/sorry-allowlist.txt — .lean files permitted to contain `sorry`.
# hille-yosida is sorry-free — this allowlist is intentionally EMPTY: no file may
# carry `sorry`. (The headline results are proved outright, not deferred.)
